function App() {
  return (
    <main className="mindmap-placeholder-shell">
      <section className="mindmap-placeholder-card">
        <span className="mindmap-placeholder-label">Mindmap Editor</span>
        <h1>Paused</h1>
        <p>
          This app is intentionally cleared for now and is not under active maintenance.
        </p>
        <p>
          Use <code>apps/vector-editor-web</code> for product work and <code>apps/playground</code> for runtime and renderer diagnostics.
        </p>
      </section>
    </main>
  )
}

export default App
