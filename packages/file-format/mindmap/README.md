# @venus/file-format/mindmap

Mindmap editor protocol module.

Use this package when mindmap semantics need app-specific fields while still
reusing Node + Feature base protocol from `../base`.

## Structure

- `schemas/vN`: Mindmap-specific schema versions
- `migrations`: incremental migrations
- `samples/vN`: versioned fixtures
