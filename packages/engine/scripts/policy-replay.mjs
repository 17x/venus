#!/usr/bin/env node

/**
 * Intent: provide a canonical policy replay command entrypoint.
 */
function main() {
  // AI-TEMP: placeholder replay entry while full policy replay harness is still pending;
  // remove when runtime policy replay harness lands in packages/engine/src/testing;
  // ref ai/operations/engine-canonical-governance-readiness-2026-05-20.md
  console.log(
    "[policy:replay] PASS: no-op placeholder while replay harness is pending.",
  );
}

main();
