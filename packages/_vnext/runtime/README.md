# @venus/runtime-staging

Staging package for runtime adapter contracts in split plan.

Scope in this bootstrap:

1. Defines adapter contracts only.
2. Does not move runtime behavior from app/engine yet.
3. Keeps package boundary explicit for future contract tests.
