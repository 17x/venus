# @venus/renderer-shared-staging

Staging shared backend contract package for renderer split.

Scope in this bootstrap:

1. Defines shared execution contracts for `canvas2d`, `webgl`, and `webgpu` backends.
2. Adds contract conformance tests for mode coverage and descriptor stability.
3. Does not move engine backend execution logic in this step.
