# Product Scope

## In Scope (Current Stage)

- Vector editor capability buildout in `apps/vector-editor-web`
- Runtime and renderer verification in `apps/playground`
- Shared package evolution in `packages/runtime`, `packages/engine`, and
  `packages/document-core`

## Out Of Scope (Current Stage)

- Full production collaboration UX
- Final mindmap and whiteboard product UX completion
- Cross-product unified design system finalization

## Phase Focus

- Priority sequence: `connector` -> `boolean`
- Maintain stable runtime/engine/app boundaries while adding capabilities
